{
  "name": "bakery",
  "private": true,
  "description": "Tortenkatalog — backend, storefront and admin panel",
  "scripts": {
    "dev": "concurrently -n backend,site,admin -c yellow,cyan,magenta \"npm:dev:backend\" \"npm:dev:site\" \"npm:dev:admin\"",
    "dev:backend": "npm --prefix backend run start:dev",
    "dev:site": "npm --prefix frontend run dev",
    "dev:admin": "npm --prefix admin run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build && npm --prefix admin run build",
    "install:all": "npm --prefix backend install && npm --prefix frontend install && npm --prefix admin install",
    "seed": "npm --prefix backend run seed && npm --prefix backend run seed:products"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
